# B-004 Validation

Valid. OAuth state records platform `userId`; callback exchanges code and stores the resulting token under that user. The code path does not verify the WPS account subject or compare it to a trusted platform-user mapping.
