# B-002 Validation

Valid when the WPS app credential has visibility beyond the caller's intended file set. No local code checks that the file ID belongs to the business system, user, or signed grant before calling WPS preview creation.
